Controller: Cli
"" get -> {
    slotCount: usually 6, but worth having just in case,
    scrollTarget: index of the day that should be in the middle of the viewport, starting from zero, zero being the first day of the schedule that you get from this call
    data: a prerendered table containing all the data for the entire semester. Brotli encoded, must be decoded.
} | NotAuthorized
"login" get (username, password) -> session ID hash string of length 32 | authenification fail

Controller: Planner
"" patch (Dictionary<string, int> titles, List<string> strategies) -> { success, failure } integers


I must be able to get the log in, get the schedule, plan stuff, reset planning, submit plan, log out.

Viewport is a table: table header at the top, a table view just below it, without empty lines, scrolled such that the scroll target is in the middle or as close as possible to the middle. I should be able to scroll using input commands for scrolling up and down (like page up or page down as a written out command). The table should span a fixed number of rows, such that it's possibly truncated at the top and at the bottom.
