You are worker A, one of 4 parallel agents fixing bugs in MyTimetable.Cli/main.c. Your branch is fix-A-shlexit, checked out in the worktree at C:\Users\PCyur\source\repos\MyTimetable-A.

Your task: Fix the shell-escaping in self_patch_any.

The function builds a cmd.exe command line with the binary path, but only escapes backslashes. Characters like &, |, ^, >, < inside the path would break the cmd.exe invocation. Add proper cmd.exe escaping.

File to edit: C:\Users\PCyur\source\repos\MyTimetable-A\MyTimetable.Cli\main.c

Specifically, in the `self_patch_any` function, find the block that builds `esc_self`:
```c
    char esc_self[MAX_PATH_A * 2] = {0};
    {
        const char* s = own_path();
        char* d = esc_self;
        while (*s) {
            if (*s == '\\') *d++ = '\\';
            *d++ = *s++;
        }
    }
```

Replace it with proper cmd.exe escaping: escape & | ^ > < characters with ^ prefix, double backslash as \\, and leave the existing backslash-doubling for the snprintf format.

After editing, verify the file still compiles conceptually (no syntax errors visible), then write "DONE" to C:\Users\PCyur\source\repos\MyTimetable-A\.task-complete

Do NOT touch any other file or branch. Only edit main.c in your worktree.
