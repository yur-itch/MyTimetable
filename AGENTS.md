# File Safety

- Never delete files you did not create in this session. If the agent didn't write or generate it, don't touch it.
- Before `rm` with wildcards or on directories, check each target: is it yours?
- Test artifacts go in `_scratch/`, never in source directories.
