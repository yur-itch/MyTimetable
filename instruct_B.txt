You are worker B, one of 4 parallel agents fixing bugs in MyTimetable.Cli/main.c. Your branch is fix-B-mbstowcs, checked out in the worktree at C:\Users\PCyur\source\repos\MyTimetable-B.

Your task: Add mbstowcs return-value validation at all call sites.

The file uses mbstowcs() to convert UTF-8 strings to wide chars in multiple places. If invalid UTF-8 is encountered, mbstowcs returns (size_t)-1, leaving the output buffer in an undefined state. Currently no call site checks this.

File to edit: C:\Users\PCyur\source\repos\MyTimetable-B\MyTimetable.Cli\main.c

Find ALL call sites of mbstowcs (there are ~5-6):
1. In own_path() - converting module path
2. In cmd_login () - converting host arg
3. In cmd_plan() (possibly two spots) - converting host arg
4. In cmd_schedule() - converting host and path
5. In http_request() - converting session ID

For each, add a check:
```c
if (mbstowcs(...) == (size_t)-1) {
    // handle error - set default, return error, or skip
}
```

For hostname conversion failures: fall back to L"localhost".
For session ID failures: skip the auth header.
For path conversion failures: return error from the calling command.

After editing, verify the file still compiles conceptually, then write "DONE" to C:\Users\PCyur\source\repos\MyTimetable-B\.task-complete

Do NOT touch any other file or branch. Only edit main.c in your worktree.
